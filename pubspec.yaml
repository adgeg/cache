name: cache
description: Simple HTTP cache.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: 3.2.0
  flutter: 3.16.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  dio: ^5.3.3
  dio_cache_interceptor: "3.4.4"
  flutter_native_splash: "2.3.6"
  flutter_launcher_icons: "0.13.1"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
